generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Student {
  id              Int       @id @default(autoincrement())
  um_email        String    @unique
  first_name      String
  last_name       String
  mid_name        String?
  personal_email  String
}
